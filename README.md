# Калькулятор натяжных стен

Адаптивное одностраничное приложение для расчёта объектов, комнат и отдельных стен. Прайс отделён от расчётной логики, а проекты сохраняются в `localStorage` браузера.

```bash
npm install
npm run dev
```

Проверки:

```bash
npm test
npm run build
```
